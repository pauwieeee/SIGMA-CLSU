VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# NOTE: this key is bundled into the client-side JS and is publicly visible
# on the deployed site (anyone can read it via browser dev tools). That's a
# deliberate tradeoff made for this project — see README for details.
VITE_GEMINI_API_KEY=your-gemini-api-key
